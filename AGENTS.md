# Workspace Agent Instructions

Before doing any task in this workspace, read `DESIGN.md` first.

Then read the target subproject's `AGENTS.md` and relevant files before making
changes. This workspace contains HyperFrames composition projects, so use the
relevant HyperFrames skill before editing video compositions and run the
subproject check command after composition changes.

This workspace is shared with GitHub Copilot. Keep durable project guidance in
`DESIGN.md` so Codex and Copilot follow the same design and verification rules.
